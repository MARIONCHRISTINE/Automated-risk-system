<?php
// Database and session code remains the same as original
?>
<!DOCTYPE html>
<html>
<head>
  <title>Dashboard | Risk Management System</title>
  <meta charset="utf-8">
  <style>
    :root {
      --primary-red: #e53935;
      --dark-red: #c62828;
      --light-red: #ffcdd2;
      --pale-red: #ffebee;
      --white: #ffffff;
      --dark-text: #263238;
      --light-text: #546e7a;
      --shadow: 0 4px 12px rgba(229, 57, 53, 0.15);
    }
    
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      font-family: 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', sans-serif;
    }
    
    body {
      background-color: var(--pale-red);
      color: var(--dark-text);
      display: flex;
      min-height: 100vh;
    }
    
    /* Sidebar */
    .sidebar {
      width: 240px;
      background: var(--white);
      box-shadow: 2px 0 10px rgba(0,0,0,0.05);
      position: fixed;
      height: 100vh;
      padding-top: 70px;
      border-right: 1px solid var(--light-red);
      transition: all 0.3s;
    }
    
    .sidebar-header {
      padding: 20px;
      border-bottom: 1px solid var(--light-red);
      margin-bottom: 10px;
    }
    
    .sidebar-menu {
      list-style: none;
    }
    
    .sidebar-menu li a {
      display: flex;
      align-items: center;
      padding: 12px 20px;
      color: var(--dark-text);
      text-decoration: none;
      transition: all 0.2s;
      border-left: 4px solid transparent;
    }
    
    .sidebar-menu li a:hover,
    .sidebar-menu li a.active {
      background: var(--pale-red);
      color: var(--primary-red);
      border-left: 4px solid var(--primary-red);
    }
    
    .sidebar-menu li a i {
      margin-right: 10px;
      width: 24px;
      text-align: center;
    }
    
    /* Main Content */
    .main-content {
      flex: 1;
      margin-left: 240px;
      padding: 20px;
    }
    
    /* Header */
    .header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 15px 25px;
      background: var(--white);
      box-shadow: var(--shadow);
      border-radius: 8px;
      margin-bottom: 25px;
    }
    
    .header-title h1 {
      color: var(--primary-red);
      font-size: 1.5rem;
      font-weight: 600;
    }
    
    .header-user {
      display: flex;
      align-items: center;
    }
    
    .header-user span {
      margin-right: 15px;
      color: var(--primary-red);
      font-weight: 500;
    }
    
    .logout-btn {
      background: var(--primary-red);
      color: var(--white);
      border: none;
      padding: 8px 16px;
      border-radius: 4px;
      cursor: pointer;
      font-weight: 500;
      transition: background 0.2s;
    }
    
    .logout-btn:hover {
      background: var(--dark-red);
    }
    
    /* Dashboard Cards */
    .dashboard-cards {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
      gap: 20px;
      margin-bottom: 30px;
    }
    
    .card {
      background: var(--white);
      border-radius: 8px;
      padding: 20px;
      box-shadow: var(--shadow);
      transition: transform 0.2s;
      border-top: 3px solid var(--primary-red);
      position: relative;
      overflow: hidden;
    }
    
    .card:hover {
      transform: translateY(-5px);
    }
    
    .card-icon {
      position: absolute;
      right: 20px;
      top: 20px;
      font-size: 2.5rem;
      opacity: 0.1;
      color: var(--primary-red);
    }
    
    .card-title {
      color: var(--light-text);
      font-size: 0.9rem;
      margin-bottom: 5px;
    }
    
    .card-value {
      color: var(--primary-red);
      font-size: 2rem;
      font-weight: 700;
      margin: 10px 0;
    }
    
    /* Chart Section */
    .chart-container {
      background: var(--white);
      border-radius: 8px;
      padding: 25px;
      box-shadow: var(--shadow);
      margin-top: 30px;
    }
    
    .chart-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 20px;
    }
    
    .chart-title {
      color: var(--primary-red);
      font-weight: 600;
    }
    
    /* Responsive */
    @media (max-width: 992px) {
      .sidebar {
        width: 70px;
        overflow: hidden;
      }
      
      .sidebar-menu li a span {
        display: none;
      }
      
      .sidebar-menu li a i {
        margin-right: 0;
        font-size: 1.2rem;
      }
      
      .main-content {
        margin-left: 70px;
      }
    }
    
    @media (max-width: 768px) {
      .dashboard-cards {
        grid-template-columns: 1fr 1fr;
      }
    }
    
    @media (max-width: 576px) {
      .dashboard-cards {
        grid-template-columns: 1fr;
      }
      
      .header {
        flex-direction: column;
        align-items: flex-start;
      }
      
      .header-user {
        margin-top: 10px;
      }
    }
  </style>
</head>
<body>
  <!-- Sidebar -->
  <div class="sidebar">
    <div class="sidebar-header">
      <h3 style="color: var(--primary-red);">Menu</h3>
    </div>
    <ul class="sidebar-menu">
      <li><a href="dashboard.php" class="active"><i>📊</i><span>Dashboard</span></a></li>
      <li><a href="users.php"><i>👥</i><span>User Management</span></a></li>
      <li><a href="invite_user.php"><i>✉️</i><span>Invite User</span></a></li>
      <li><a href="audit_logs.php"><i>📝</i><span>Audit Logs</span></a></li>
      <li><a href="backup.php"><i>💾</i><span>Backup</span></a></li>
    </ul>
  </div>

  <!-- Main Content -->
  <div class="main-content">
    <!-- Header -->
    <div class="header">
      <div class="header-title">
        <h1>Risk Management Dashboard</h1>
      </div>
      <div class="header-user">
        <span>👤 <?= htmlspecialchars($user['name']) ?></span>
        <button class="logout-btn" onclick="window.location.href='logout.php'">Logout</button>
      </div>
    </div>

    <!-- Dashboard Cards -->
    <div class="dashboard-cards">
      <div class="card">
        <div class="card-icon">👤</div>
        <div class="card-title">Active Users</div>
        <div class="card-value"><?= $active_users ?></div>
      </div>
      
      <div class="card">
        <div class="card-icon">🔒</div>
        <div class="card-title">Locked Accounts</div>
        <div class="card-value"><?= $locked_accounts ?></div>
      </div>
      
      <div class="card">
        <div class="card-icon">✉️</div>
        <div class="card-title">Pending Invites</div>
        <div class="card-value"><?= $pending_invites ?></div>
      </div>
      
      <div class="card">
        <div class="card-icon">⏰</div>
        <div class="card-title">Recent Logins</div>
        <div class="card-value"><?= $recent_logins ?></div>
      </div>
    </div>

    <!-- Chart Section -->
    <div class="chart-container">
      <div class="chart-header">
        <h2 class="chart-title">Login Trends (Past 7 Days)</h2>
      </div>
      <canvas id="loginChart" height="100"></canvas>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
  <script>
    // Chart Configuration
    const ctx = document.getElementById('loginChart').getContext('2d');
    const chart = new Chart(ctx, {
      type: 'line',
      data: {
        labels: <?= $labels_json ?>,
        datasets: [{
          label: 'Logins',
          data: <?= $data_json ?>,
          backgroundColor: 'rgba(229, 57, 53, 0.1)',
          borderColor: 'rgba(229, 57, 53, 1)',
          borderWidth: 2,
          tension: 0.4,
          pointBackgroundColor: '#fff',
          pointBorderColor: 'rgba(229, 57, 53, 1)',
          pointRadius: 4,
          pointHoverRadius: 6,
          fill: true
        }]
      },
      options: {
        responsive: true,
        plugins: {
          legend: {
            display: false
          },
          tooltip: {
            backgroundColor: '#fff',
            titleColor: '#e53935',
            bodyColor: '#263238',
            borderColor: '#e53935',
            borderWidth: 1,
            padding: 12,
            boxShadow: '0 4px 12px rgba(0,0,0,0.1)'
          }
        },
        scales: {
          y: {
            beginAtZero: true,
            grid: {
              color: 'rgba(0,0,0,0.05)'
            },
            ticks: {
              color: '#546e7a'
            }
          },
          x: {
            grid: {
              display: false
            },
            ticks: {
              color: '#546e7a'
            }
          }
        }
      }
    });

    // Inactivity Timer
    let inactivityTimer;
    const logoutTime = 30 * 60 * 1000; // 30 minutes
    
    function resetTimer() {
      clearTimeout(inactivityTimer);
      inactivityTimer = setTimeout(logout, logoutTime);
    }
    
    function logout() {
      window.location.href = 'logout.php?inactive=1';
    }
    
    // Event listeners
    window.onload = resetTimer;
    document.onmousemove = resetTimer;
    document.onkeypress = resetTimer;
    document.onclick = resetTimer;
    document.onscroll = resetTimer;
  </script>
</body>
</html>
