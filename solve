ALTER TABLE `risk_incidents` 
ADD COLUMN `risk_rating` DECIMAL(10,2) DEFAULT NULL COMMENT 'Calculated risk rating (likelihood × impact)' AFTER `residual_rating`;
ALTER TABLE `risk_incidents` 
ADD COLUMN `involves_money_loss` VARCHAR(10) NULL DEFAULT NULL COMMENT 'Whether the risk involves money loss' AFTER `treatment_documents`,
ADD COLUMN `money_amount` DECIMAL(15,2) NULL DEFAULT NULL COMMENT 'Estimated amount of money loss' AFTER `involves_money_loss`;
